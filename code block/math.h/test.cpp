#include<iostream>
#include <math.h>
#include <typeinfo>
using namespace std;

int main(){
double x=10.1;
int y=10;
 x=log(x);

double log[y];
cout << typeid(log[y]).name() << endl;
long double logl(long double x);

cout<<x;
return 0;
}
